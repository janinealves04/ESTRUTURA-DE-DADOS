/*class SearchTree {
    constructor(){
        this.root = null;
        this.nodeEsquesda = null;
        this.nodeDireita = null;
    }
//buscar 
    
    SearchTreeNode(node = this.root, valor ){
        if(this.root !== null){
            if (node === valor){
                node = valor;
                console.log(${valor});
            } 
            else {
                if (this.nodeEsquerda === valor){
                    console.log(${valor});
                }
            }
        }
       


    };

    
};

*/

// node é diferente de nulo? Se sim, prossegue.
// node você é o valor que eu quero? se sim printa valor encontrado. 
//Se não for eu pulo pro próximo no esqueda ou direta 


 class BinarySearchTree {
     constructor(){

     }

     search (value){
        return false ;
     }
 }