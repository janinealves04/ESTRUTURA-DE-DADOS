/*
1. Utilizando uma pilha, escreva uma função em JavaScript que inverta os caracteres de uma string.
2. Escreva uma função em JavaScript que utilize uma pilha para determinar se uma string contendo apenas '(', ')', '{', '}', '[' e ']' é válida. A string é válida se todos os tipos de parênteses são fechados na ordem correta.
3. Escreva uma função em JavaScript que simplifique o caminho de um diretório. O caminho do diretório será dado como uma string, que pode incluir **`'..'`**, que se refere ao diretório pai, e **`'.'`**, que se refere ao diretório atual. Por exemplo, o caminho **`"/home//usuario/../usuario2/./"`** deve ser simplificado para **`"/home/usuario2"`**.

*/